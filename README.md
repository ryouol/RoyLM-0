# RoyLM-0 is a charcter-level GPT from scratch 

raw text -> charcters -> integer IDs -> embeddings -> transformer blocks -> logits -> loss -> training -> sampling text

Tiny decoder only transformer trained on Tiny shakespere
